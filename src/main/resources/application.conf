ktor{
     deployment{
         port=9000
         port=${?PORT }
    }
    application{
        modules=[ModuleKt.module]
    }
}

storage{
    jdbcURL = "jdbc:postgresql://localhost:5432/postgres"
    username = "postgres"
    pswd = "pass"
    maxConnPoolSize=10
}